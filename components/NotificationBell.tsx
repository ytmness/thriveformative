"use client";

/**
 * Notifications used to load from Supabase. Patients no longer have site accounts;
 * contact alerts are handled in Admin → Solicitudes (Postgres).
 */
export default function NotificationBell() {
  return null;
}
